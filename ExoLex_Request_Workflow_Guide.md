# 📋 ExoLex - دليل رحلة الطلبات والعروض
## Request & Quote Workflow Guide v1.0
### تاريخ الإنشاء: 5 يناير 2026

---

# 📑 فهرس المحتويات

1. [أنواع الطلبات](#1-أنواع-الطلبات)
2. [رحلة طلبات الباقة](#2-رحلة-طلبات-الباقة)
3. [رحلة الخدمات الإضافية](#3-رحلة-الخدمات-الإضافية)
4. [رحلة طلبات عروض الأسعار](#4-رحلة-طلبات-عروض-الأسعار)
5. [الجداول والحقول](#5-الجداول-والحقول)
6. [أمثلة عملية](#6-أمثلة-عملية)

---

# 1. أنواع الطلبات

## 1.1 تصنيف الطلبات حسب المصدر

| النوع | المصدر (`source`) | الدفع | المُعالج |
|-------|------------------|-------|----------|
| استشارة من الباقة | `package` | مشمول بالاشتراك | الذراع القانوني فقط |
| قضية من الباقة | `package` | مشمول بالاشتراك | الذراع القانوني فقط |
| خدمة إضافية (سعر ثابت) | `extra_services_page` | مدفوع | شريك أو مستقل |
| خدمة إضافية (تحتاج تسعير) | `extra_services_page` | يُحدد لاحقاً | شريك أو مستقل |
| طلب من NOLEX | `nolex` | حسب النوع | حسب النوع |

## 1.2 أولوية التوزيع

```
┌─────────────────────────────────────────────────────────────────┐
│                   أولوية توزيع الطلبات                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  طلبات الباقة (استشارات/قضايا):                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 1️⃣ الذراع القانوني (فقط) ────────────────────────────▶ │   │
│  │    إذا غير متاح ──────────────▶ ⚠️ قائمة الانتظار      │   │
│  │    ❌ لا يذهب للشريك أو المستقل أبداً                   │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  الخدمات الإضافية (المدفوعة):                                   │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 1️⃣ الشريك النشط ──────────────────────────────────────▶ │   │
│  │ 2️⃣ المحامي المستقل ───────────────────────────────────▶ │   │
│  │ 3️⃣ الذراع القانوني (إذا لم يتوفر غيره) ───────────────▶ │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

# 2. رحلة طلبات الباقة

## 2.1 مسار الاستشارة/القضية المشمولة

```
┌──────────────┐     ┌────────────────────┐     ┌────────────────┐
│   المشترك    │────▶│  إنشاء الطلب       │────▶│  التحقق من    │
│  يطلب خدمة   │     │  service_requests  │     │  الرصيد المتاح │
└──────────────┘     └────────────────────┘     └────────────────┘
                                                        │
                     ┌──────────────────────────────────┘
                     ▼
              ┌─────────────┐
              │ رصيد كافي؟  │
              └─────────────┘
                │         │
         نعم ✅ │         │ ❌ لا
                ▼         ▼
    ┌───────────────┐  ┌─────────────────────┐
    │ خصم من الرصيد │  │ رفض + عرض شراء     │
    │ consultations │  │ خدمة إضافية         │
    │ _remaining    │  └─────────────────────┘
    └───────────────┘
            │
            ▼
    ┌───────────────────┐
    │ توزيع للذراع      │
    │ القانوني          │
    └───────────────────┘
            │
            ▼
    ┌───────────────────┐     ┌───────────────────┐
    │ محامي الذراع     │────▶│ قبول الطلب        │
    │ يرى العرض        │     │ status: accepted  │
    └───────────────────┘     └───────────────────┘
            │
            ▼
    ┌───────────────────┐     ┌───────────────────┐
    │ تنفيذ الخدمة      │────▶│ إغلاق الطلب       │
    │ status:          │     │ status: completed │
    │ in_progress      │     └───────────────────┘
    └───────────────────┘
```

## 2.2 حالات طلب الباقة

| الحالة (`status`) | الوصف | الإجراء التالي |
|-------------------|-------|----------------|
| `new` | طلب جديد | التحقق من الرصيد |
| `pending_assignment` | بانتظار التعيين | توزيع للذراع |
| `assigned` | تم التعيين | انتظار قبول المحامي |
| `accepted` | تم القبول | بدء العمل |
| `in_progress` | قيد التنفيذ | إكمال الخدمة |
| `completed` | مكتمل | التقييم |
| `cancelled` | ملغي | - |

## 2.3 الحقول المستخدمة في service_requests

| الحقل | الوصف | القيمة |
|-------|-------|--------|
| `source` | مصدر الطلب | `package` |
| `request_type` | نوع الطلب | `consultation` / `case` |
| `handler_type` | نوع المُعالج | `legal_arm` |
| `assigned_lawyer_id` | معرف المحامي | UUID من `lawyers` |
| `status` | الحالة | (انظر الجدول أعلاه) |
| `credit_consumed` | تم خصم الرصيد | `true` |

---

# 3. رحلة الخدمات الإضافية

## 3.1 نوعان من الخدمات الإضافية

### أ) خدمة بسعر ثابت (Fixed Price)
- السعر محدد مسبقاً
- الدفع مباشر عند الطلب
- توزيع فوري

### ب) خدمة تحتاج تسعير (Quote Required)
- السعر غير محدد
- يُطلب عرض سعر من المحامين
- المشترك يختار العرض المناسب

## 3.2 مسار الخدمة ذات السعر الثابت

```
┌──────────────┐     ┌────────────────────┐     ┌────────────────┐
│   المشترك    │────▶│  اختيار الخدمة     │────▶│  الدفع         │
│              │     │  extra_services    │     │  (Tap/Moyasar) │
└──────────────┘     └────────────────────┘     └────────────────┘
                                                        │
                                                        ▼
                                              ┌────────────────┐
                                              │ إنشاء الطلب    │
                                              │service_requests│
                                              │source:         │
                                              │extra_services_ │
                                              │page            │
                                              └────────────────┘
                                                        │
                                                        ▼
                                              ┌────────────────┐
                                              │ إنشاء العرض    │
                                              │service_offers  │
                                              │target_type:    │
                                              │all             │
                                              └────────────────┘
                                                        │
                              ┌──────────────────────────┴─────────────────────┐
                              ▼                                                ▼
                    ┌────────────────┐                              ┌────────────────┐
                    │ الشريك يرى    │                              │ المستقل يرى   │
                    │ العرض          │                              │ العرض          │
                    └────────────────┘                              └────────────────┘
                              │                                                │
                              └──────────────────────┬─────────────────────────┘
                                                     ▼
                                           ┌────────────────┐
                                           │ أول من يقبل   │
                                           │ يحصل على      │
                                           │ الطلب          │
                                           └────────────────┘
                                                     │
                                                     ▼
                                           ┌────────────────┐
                                           │ تنفيذ + إغلاق │
                                           └────────────────┘
```

## 3.3 جدول service_offers

| الحقل | الوصف | القيم الممكنة |
|-------|-------|---------------|
| `id` | المعرف | UUID |
| `request_id` | الطلب المرتبط | FK → service_requests.id |
| `price` | السعر | رقم |
| `lawyer_commission_percentage` | نسبة عمولة المحامي | 70% |
| `lawyer_commission_amount` | مبلغ العمولة | محسوب |
| `target_type` | الاستهداف | `independent` / `arm` / `all` |
| `status` | الحالة | `pending` / `accepted` / `expired` / `cancelled` |
| `priority` | الأولوية | `low` / `normal` / `high` / `urgent` |
| `expires_at` | انتهاء العرض | 4 ساعات |
| `accepted_by` | من قبل العرض | UUID |
| `accepted_at` | وقت القبول | timestamp |

## 3.4 حالات العرض (service_offers.status)

```
┌──────────┐     ┌──────────┐
│ pending  │────▶│ accepted │ ✅ تم قبول العرض
└──────────┘     └──────────┘
     │
     │           ┌──────────┐
     └──────────▶│ expired  │ ⏰ انتهت الصلاحية
     │           └──────────┘
     │
     │           ┌───────────┐
     └──────────▶│ cancelled │ ❌ ملغي
                 └───────────┘
```

---

# 4. رحلة طلبات عروض الأسعار

## 4.1 متى يُستخدم؟

- خدمات ليس لها سعر ثابت
- خدمات معقدة تحتاج تقييم
- المشترك يريد مقارنة الأسعار

## 4.2 المسار الكامل

```
┌───────────────────────────────────────────────────────────────────────────────┐
│                          رحلة طلب عرض السعر                                   │
├───────────────────────────────────────────────────────────────────────────────┤
│                                                                               │
│  ┌──────────────┐                                                             │
│  │   المشترك    │                                                             │
│  │ يطلب خدمة   │                                                             │
│  │ (تحتاج تسعير)│                                                             │
│  └──────┬───────┘                                                             │
│         │                                                                     │
│         ▼                                                                     │
│  ┌──────────────────┐                                                         │
│  │ إنشاء طلب عرض   │                                                         │
│  │ quote_requests   │                                                         │
│  │ status: pending  │                                                         │
│  └──────┬───────────┘                                                         │
│         │                                                                     │
│         ▼                                                                     │
│  ┌──────────────────────────────────────────────────────┐                    │
│  │           يُرسل لجميع المحامين المؤهلين              │                    │
│  └──────────────────────────────────────────────────────┘                    │
│         │                    │                    │                           │
│         ▼                    ▼                    ▼                           │
│  ┌────────────┐      ┌────────────┐      ┌────────────┐                      │
│  │ محامي 1   │      │ محامي 2   │      │ محامي 3   │                      │
│  └────────────┘      └────────────┘      └────────────┘                      │
│         │                    │                    │                           │
│         ▼                    ▼                    ▼                           │
│  ┌────────────┐      ┌────────────┐      ┌────────────┐                      │
│  │تقديم عرض  │      │طلب تفاصيل │      │تجاهل      │                      │
│  │lawyer_    │      │quote_detail│      │           │                      │
│  │quotes     │      │_requests   │      │           │                      │
│  └─────┬──────┘      └─────┬──────┘      └────────────┘                      │
│        │                   │                                                  │
│        │                   ▼                                                  │
│        │            ┌────────────┐                                           │
│        │            │ المشترك   │                                           │
│        │            │ يرد       │                                           │
│        │            └─────┬──────┘                                           │
│        │                  │                                                   │
│        │                  ▼                                                   │
│        │            ┌────────────┐                                           │
│        │            │ المحامي   │                                           │
│        │            │يقدم عرضه  │                                           │
│        │            └─────┬──────┘                                           │
│        │                  │                                                   │
│        └────────┬─────────┘                                                   │
│                 ▼                                                             │
│  ┌──────────────────────────────────────────────────────┐                    │
│  │           المشترك يرى جميع العروض                    │                    │
│  │           ويختار الأنسب                              │                    │
│  └──────────────────────────────────────────────────────┘                    │
│                 │                                                             │
│                 ▼                                                             │
│  ┌──────────────────┐      ┌──────────────────┐                              │
│  │ العرض المختار    │      │ باقي العروض     │                              │
│  │ status: selected │      │ status:          │                              │
│  │                  │      │ not_selected     │                              │
│  └──────┬───────────┘      └──────────────────┘                              │
│         │                                                                     │
│         ▼                                                                     │
│  ┌──────────────────┐                                                         │
│  │ الدفع            │                                                         │
│  └──────┬───────────┘                                                         │
│         │                                                                     │
│         ▼                                                                     │
│  ┌──────────────────┐                                                         │
│  │ إنشاء طلب خدمة  │                                                         │
│  │ service_requests │                                                         │
│  │ مع المحامي      │                                                         │
│  │ المختار         │                                                         │
│  └──────┬───────────┘                                                         │
│         │                                                                     │
│         ▼                                                                     │
│  ┌──────────────────┐                                                         │
│  │ تنفيذ الخدمة    │                                                         │
│  └──────────────────┘                                                         │
│                                                                               │
└───────────────────────────────────────────────────────────────────────────────┘
```

## 4.3 جدول quote_requests (طلب عرض السعر)

| الحقل | الوصف | ملاحظات |
|-------|-------|---------|
| `id` | المعرف | UUID |
| `request_number` | رقم الطلب | للعرض |
| `member_id` | المشترك | FK → members.id |
| `member_code` | رقم المشترك | للعرض |
| `service_type` | نوع الخدمة | |
| `domain` | المجال القانوني | |
| `category` | التصنيف | |
| `title` | العنوان | |
| `description` | الوصف | |
| `attachments` | المرفقات | JSONB |
| `deadline` | الموعد المطلوب | |
| `status` | الحالة | (انظر أدناه) |
| `awarded_lawyer_id` | المحامي الفائز | UUID |
| `awarded_quote_id` | العرض الفائز | UUID |
| `awarded_at` | وقت الترسية | |

### حالات quote_requests.status:

| الحالة | الوصف |
|--------|-------|
| `pending` | بانتظار العروض |
| `has_quotes` | وصلت عروض |
| `awarded` | تم اختيار عرض |
| `expired` | انتهت المدة |
| `cancelled` | ملغي |

## 4.4 جدول lawyer_quotes (عرض المحامي)

| الحقل | الوصف | ملاحظات |
|-------|-------|---------|
| `id` | المعرف | UUID |
| `quote_request_id` | طلب العرض | FK → quote_requests.id |
| `lawyer_id` | المحامي | FK → lawyers.id |
| `service_description` | وصف الخدمة | |
| `total_price` | السعر الإجمالي | |
| `platform_fee` | رسوم المنصة | 30% |
| `lawyer_net` | صافي المحامي | 70% |
| `vat_amount` | الضريبة | 15% |
| `total_with_vat` | الإجمالي مع الضريبة | |
| `payment_type` | نوع الدفع | `full` / `installment` |
| `payment_schedule` | جدول الأقساط | JSONB |
| `status` | الحالة | (انظر أدناه) |
| `expires_at` | انتهاء العرض | 48 ساعة |
| `responded_at` | وقت الرد | |
| `rejection_reason` | سبب الرفض | |

### حالات lawyer_quotes.status:

| الحالة | الوصف |
|--------|-------|
| `draft` | مسودة |
| `submitted` | تم التقديم |
| `selected` | تم اختياره ✅ |
| `not_selected` | لم يُختر |
| `expired` | انتهى |
| `withdrawn` | سحبه المحامي |

## 4.5 جدول quote_detail_requests (طلب تفاصيل)

| الحقل | الوصف | ملاحظات |
|-------|-------|---------|
| `id` | المعرف | UUID |
| `quote_request_id` | طلب العرض | FK → quote_requests.id |
| `lawyer_quote_id` | عرض المحامي | FK → lawyer_quotes.id (nullable) |
| `sender_type` | نوع المُرسل | `lawyer` / `member` |
| `sender_id` | المُرسل | UUID |
| `question` | السؤال | |
| `answer` | الجواب | |
| `answered_at` | وقت الرد | |
| `status` | الحالة | `pending` / `answered` |

---

# 5. الجداول والحقول

## 5.1 خريطة الجداول

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              خريطة الجداول                                  │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌─────────────────┐                                                        │
│  │ service_requests│◀────────────────────────────────────────────┐         │
│  │ (الطلبات)       │                                             │         │
│  └────────┬────────┘                                             │         │
│           │                                                       │         │
│           │ request_id                                            │         │
│           ▼                                                       │         │
│  ┌─────────────────┐     ┌─────────────────┐                     │         │
│  │ service_offers  │     │ quote_requests  │                     │         │
│  │ (عروض الخدمات)  │     │ (طلبات التسعير) │                     │         │
│  └────────┬────────┘     └────────┬────────┘                     │         │
│           │                       │                               │         │
│           │                       │ quote_request_id              │         │
│           │                       ▼                               │         │
│           │              ┌─────────────────┐                     │         │
│           │              │ lawyer_quotes   │                     │         │
│           │              │ (عروض المحامين) │                     │         │
│           │              └────────┬────────┘                     │         │
│           │                       │                               │         │
│           │                       │ lawyer_quote_id               │         │
│           │                       ▼                               │         │
│           │              ┌─────────────────────┐                 │         │
│           │              │ quote_detail_       │                 │         │
│           │              │ requests            │                 │         │
│           │              │ (طلبات التفاصيل)    │                 │         │
│           │              └─────────────────────┘                 │         │
│           │                                                       │         │
│           │ accepted_by / lawyer_id                              │         │
│           ▼                                                       │         │
│  ┌─────────────────┐                                             │         │
│  │    lawyers      │◀────────────────────────────────────────────┘         │
│  │  (المحامين)     │                                                       │
│  └─────────────────┘                                                       │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

## 5.2 ملخص الحقول المهمة

### service_requests (الطلب الرئيسي)
```sql
- id (UUID)
- ticket_number (SVC-YY-XXXXXX)
- member_id → members.id ⚠️
- source: 'package' | 'extra_services_page' | 'nolex'
- request_type: 'consultation' | 'case' | 'extra_service'
- handler_type: 'legal_arm' | 'partner' | 'independent'
- assigned_lawyer_id → lawyers.id
- assigned_partner_id → partners.id
- status: 'new' | 'pending_assignment' | 'assigned' | 'accepted' | 'in_progress' | 'completed' | 'cancelled'
```

### service_offers (عرض الخدمة)
```sql
- id (UUID)
- request_id → service_requests.id
- price (السعر)
- lawyer_commission_percentage (70%)
- target_type: 'independent' | 'arm' | 'all'
- status: 'pending' | 'accepted' | 'expired' | 'cancelled'
- priority: 'low' | 'normal' | 'high' | 'urgent'
- expires_at (4 ساعات)
- accepted_by → lawyers.id
```

### quote_requests (طلب عرض سعر)
```sql
- id (UUID)
- request_number
- member_id → members.id
- status: 'pending' | 'has_quotes' | 'awarded' | 'expired' | 'cancelled'
- awarded_lawyer_id → lawyers.id
- awarded_quote_id → lawyer_quotes.id
```

### lawyer_quotes (عرض المحامي)
```sql
- id (UUID)
- quote_request_id → quote_requests.id
- lawyer_id → lawyers.id
- total_price
- platform_fee (30%)
- lawyer_net (70%)
- vat_amount (15%)
- status: 'draft' | 'submitted' | 'selected' | 'not_selected' | 'expired' | 'withdrawn'
- expires_at (48 ساعة)
```

---

# 6. أمثلة عملية

## 6.1 مثال: استشارة من الباقة

```typescript
// 1. المشترك يطلب استشارة
const { data: request } = await supabase
  .from('service_requests')
  .insert({
    member_id: memberData.id,  // ⚠️ member_id وليس user_id
    request_type: 'consultation',
    source: 'package',
    title: 'استشارة عمالية',
    description: 'تم فصلي من العمل...',
    category_id: 'LABOR_CATEGORY_UUID',
    status: 'new',
    priority: 'normal',
    sla_hours: 24
  })
  .select()
  .single()

// 2. خصم من رصيد الاشتراك
await supabase
  .from('subscriptions')
  .update({
    consultations_remaining: subscription.consultations_remaining - 1
  })
  .eq('id', subscription.id)

// 3. تحديث حالة الطلب
await supabase
  .from('service_requests')
  .update({
    status: 'pending_assignment',
    credit_consumed: true
  })
  .eq('id', request.id)

// 4. توزيع للذراع القانوني
const { data: armLawyer } = await supabase
  .from('lawyers')
  .select('*')
  .eq('lawyer_type', 'legal_arm')
  .eq('status', 'active')
  .eq('is_available', true)
  .order('active_requests_count', { ascending: true })
  .limit(1)
  .single()

await supabase
  .from('service_requests')
  .update({
    assigned_lawyer_id: armLawyer.id,
    handler_type: 'legal_arm',
    status: 'assigned',
    assigned_at: new Date().toISOString()
  })
  .eq('id', request.id)
```

## 6.2 مثال: خدمة إضافية بسعر ثابت

```typescript
// 1. المشترك يختار خدمة ويدفع
// ... عملية الدفع ...

// 2. إنشاء الطلب
const { data: request } = await supabase
  .from('service_requests')
  .insert({
    member_id: memberData.id,
    request_type: 'extra_service',
    source: 'extra_services_page',
    extra_service_id: selectedService.id,
    title: selectedService.name_ar,
    base_price: selectedService.price,
    total_amount: selectedService.price * 1.15, // مع الضريبة
    status: 'pending_assignment'
  })
  .select()
  .single()

// 3. إنشاء عرض لجميع المحامين
const { data: offer } = await supabase
  .from('service_offers')
  .insert({
    request_id: request.id,
    service_name_ar: selectedService.name_ar,
    price: selectedService.price,
    lawyer_commission_percentage: 70,
    lawyer_commission_amount: selectedService.price * 0.7,
    target_type: 'all',
    status: 'pending',
    priority: 'normal',
    expires_at: new Date(Date.now() + 4 * 60 * 60 * 1000) // 4 ساعات
  })
  .select()
  .single()
```

## 6.3 مثال: طلب عرض سعر

```typescript
// 1. المشترك يطلب خدمة تحتاج تسعير
const { data: quoteRequest } = await supabase
  .from('quote_requests')
  .insert({
    member_id: memberData.id,
    member_code: memberData.member_code,
    service_type: 'contract_drafting',
    domain: 'commercial',
    title: 'صياغة عقد شراكة',
    description: 'أحتاج عقد شراكة بين 3 أطراف...',
    deadline: '2026-01-15',
    status: 'pending'
  })
  .select()
  .single()

// 2. المحامي يطلب تفاصيل إضافية
const { data: detailRequest } = await supabase
  .from('quote_detail_requests')
  .insert({
    quote_request_id: quoteRequest.id,
    sender_type: 'lawyer',
    sender_id: lawyerId,
    question: 'ما هي نسب الشراكة المتفق عليها؟',
    status: 'pending'
  })
  .select()
  .single()

// 3. المشترك يرد
await supabase
  .from('quote_detail_requests')
  .update({
    answer: 'النسب: 40% - 35% - 25%',
    answered_at: new Date().toISOString(),
    status: 'answered'
  })
  .eq('id', detailRequest.id)

// 4. المحامي يقدم عرضه
const { data: quote } = await supabase
  .from('lawyer_quotes')
  .insert({
    quote_request_id: quoteRequest.id,
    lawyer_id: lawyerId,
    service_description: 'صياغة عقد شراكة شامل يتضمن...',
    total_price: 3000,
    platform_fee: 900,  // 30%
    lawyer_net: 2100,   // 70%
    vat_amount: 450,    // 15%
    total_with_vat: 3450,
    payment_type: 'full',
    status: 'submitted',
    expires_at: new Date(Date.now() + 48 * 60 * 60 * 1000) // 48 ساعة
  })
  .select()
  .single()

// 5. المشترك يختار العرض
await supabase
  .from('lawyer_quotes')
  .update({ status: 'selected' })
  .eq('id', quote.id)

await supabase
  .from('quote_requests')
  .update({
    status: 'awarded',
    awarded_lawyer_id: lawyerId,
    awarded_quote_id: quote.id,
    awarded_at: new Date().toISOString()
  })
  .eq('id', quoteRequest.id)

// 6. إنشاء طلب الخدمة الفعلي بعد الدفع
const { data: serviceRequest } = await supabase
  .from('service_requests')
  .insert({
    member_id: memberData.id,
    request_type: 'extra_service',
    source: 'quote',
    assigned_lawyer_id: lawyerId,
    handler_type: 'independent',
    title: 'صياغة عقد شراكة',
    total_amount: 3450,
    status: 'assigned'
  })
  .select()
  .single()
```

---

# 📝 سجل التحديثات

| التاريخ | التغيير | المسؤول |
|---------|---------|---------|
| 5 يناير 2026 | إنشاء الدليل | Claude + Jassi |

---

**نهاية الدليل - الإصدار 1.0**
